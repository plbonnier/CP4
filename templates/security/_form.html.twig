{{ form_start(form) }}

<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.lastname) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.firstname) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.adress) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.zipcode) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.city) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.country) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.pictureFile) }}</div>
<div class="d-flex justify-content-evenly mt-4">{{ form_row(form.email) }}</div>

<div class="d-flex justify-content-center  mt-4">
	<button class="btn button-crud">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}

{% extends 'base.html.twig' %}

{% block title %}Home
{% endblock %}

{% block body %}
<div class="vh-100">
	<div class="d-flex flex-column align-items-center">
    <img src="{{ vich_uploader_asset(user) }}" class="img-fluid rounded-start object-fit-cover w-25" alt="photo de {{ user.firstname }} {{ user.lastname }}"/>
    
    <h1 class="mt-3">{{ user.firstname }} {{user.lastname }}</h1>

    <p class="mt-3">Email : {{ user.email }} </p>
    <p class="mt-3">Adresse : {{ user.adress}} {{ user.zipcode}} {{ user.city }} {{ user.country }}<p>

</div>
</div>
{% endblock %}

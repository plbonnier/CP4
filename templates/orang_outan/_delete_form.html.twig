<form method="post" action="{{ path('app_orang_outan_delete', {'id': orang_outan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ orang_outan.id) }}">
	<div class="d-flex justify-content-center mt-3">
		<button class="btn button-cta">Supprimer un pensionnaire</button>
	</div>
</form>

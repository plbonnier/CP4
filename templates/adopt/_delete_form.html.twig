<form method="post" action="{{ path('app_adopt_delete', {'id': adopt.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adopt.id) }}">
    <button class="btn">Delete</button>
</form>
